数据的原罪——训练数据中的历史偏见如何被算法放大

摘要：训练数据是算法系统的“食粮”，但数据并非客观现实的透明反映，而是社会过程的产物，承载着历史积累的不平等。本文分析训练数据中历史偏见的来源、类型和放大机制，探讨数据偏见如何通过算法系统被复制、放大和固化。研究表明，训练数据中的历史偏见来源于代表性不足、标注偏见、历史歧视记录和代理变量等渠道，这些偏见通过模型学习的统计规律、反馈循环的自我强化和规模效应的扩散机制被系统性放大。数据偏见具有隐蔽性、结构性和自我强化性特征，使其难以通过简单的数据清洗来矫正。治理数据偏见需要数据采集的代表性保障、标注过程的质量控制、公平性约束的模型训练和持续监测的反馈机制的多管齐下。

关键词：训练数据；历史偏见；数据偏见；放大机制；代表性不足

一、引言

算法系统的性能依赖于数据。训练数据的质量、规模和代表性，直接决定算法系统的预测能力和公平性。然而，数据并非客观现实的透明反映，而是社会过程的产物。历史数据中蕴含着长期积累的社会不平等——种族歧视、性别偏见、阶层分化——这些不平等被数据记录，成为算法学习的“素材”。

算法系统通过历史数据学习模式和规律。如果历史数据中存在系统性偏见，算法便会习得这些偏见。更令人不安的是，算法系统不仅复制偏见，还可能放大偏见。通过模型学习的统计规律、反馈循环的自我强化和规模效应的扩散机制，数据偏见可能被放大和固化，造成比原始数据更严重的歧视。

2018年，麻省理工学院研究人员发现，亚马逊的人脸识别系统对深色皮肤女性的错误识别率高达34.7%，而对浅色皮肤男性的错误识别率仅为0.8%。这一差异并非技术故障，而是训练数据中深色皮肤女性样本不足的结果。类似地，谷歌翻译在将性别中性的语言翻译为英语时，倾向于将某些职业默认标注为男性，反映了训练数据中职业性别分布的偏见。

本文试图分析训练数据中历史偏见的来源、类型和放大机制，探讨数据偏见如何通过算法系统被复制、放大和固化，并讨论治理数据偏见的可能路径。

二、训练数据中历史偏见的来源

训练数据中的历史偏见来源于多个渠道。

代表性不足是数据偏见的第一种来源。训练数据中某些群体的样本数量不足，导致算法系统对这些群体的预测能力较弱，错误率较高。人脸识别系统对深色皮肤女性的错误识别率显著高于浅色皮肤男性，部分原因在于训练数据中深色皮肤女性样本不足。代表性不足使算法系统对少数群体的预测精度下降，形成系统性的性能差异。

标注偏见是数据偏见的第二种来源。训练数据需要人工标注，而标注过程可能受标注者偏见的影响。如果标注者对某些群体存在刻板印象，标注结果便可能反映这些偏见。在情感分析、内容审核和风险评估等任务中，标注偏见可能导致算法系统对特定群体产生系统性偏见。

历史歧视记录是数据偏见的第三种来源。历史数据中蕴含着社会既有的不平等。如果历史数据中某些群体被系统性地低估、忽视或负面标记，算法便会习得这些偏见。招聘算法通过历史招聘数据训练，如果历史数据中男性占主导地位，算法便会学会贬低女性简历。信贷算法通过历史信贷数据训练，如果历史数据中少数族裔的违约率被高估，算法便会对少数族裔产生偏见。

代理变量是数据偏见的第四种来源。即使种族、性别等敏感特征被明确排除，代理变量——如邮政编码、教育背景、消费习惯——仍可能间接编码这些敏感特征。邮政编码可能编码种族隔离，教育背景可能编码阶层分化，消费习惯可能编码性别角色。代理变量使算法偏见可能在敏感特征被排除后仍然存在。

三、数据偏见的类型

训练数据中的历史偏见，可以分为多种类型。

样本偏见是数据偏见的第一种类型。样本偏见指训练样本不能代表目标总体，导致算法系统对某些群体的预测能力较弱。样本偏见可能源于数据采集的不完整、不均衡或有偏抽样。人脸识别系统对深色皮肤女性的偏见，部分源于样本偏见。

标注偏见是数据偏见的第二种类型。标注偏见指训练数据的标注结果反映标注者的偏见，而非客观事实。标注偏见可能源于标注者的刻板印象、文化背景或认知局限。情感分析系统对特定群体语言的情感误判，部分源于标注偏见。

历史偏见是数据偏见的第三种类型。历史偏见指训练数据中蕴含的历史不平等，被算法系统习得和复制。历史偏见源于社会既有的不平等，而非数据采集或标注的失误。招聘算法、信贷算法和司法算法中的偏见，部分源于历史偏见。

代理偏见是数据偏见的第四种类型。代理偏见指敏感特征被排除后，代理变量间接编码敏感特征，导致算法偏见仍然存在。代理偏见使算法偏见难以通过简单的敏感特征排除来矫正。

四、数据偏见的放大机制

数据偏见通过算法系统被放大，放大机制包括统计规律、反馈循环和规模效应。

统计规律是数据偏见放大的第一个机制。算法系统通过历史数据学习统计规律。如果历史数据中存在系统性偏见，算法便会习得这些偏见，并将其应用于新的决策。算法系统的预测能力使偏见能够被系统性地应用于新的个体，而非仅停留在历史数据之中。统计规律使数据偏见从历史记录转化为现实决策。

反馈循环是数据偏见放大的第二个机制。算法系统的决策影响现实世界，现实世界的变化产生新的数据，新的数据用于算法的再训练。如果算法系统对某些群体存在偏见，这些偏见可能通过反馈循环被放大和固化。预测性警务算法将更多警力部署到少数族裔社区，这些社区产生更多逮捕记录，这些记录成为算法再训练的数据，进一步强化对少数族裔社区的偏见。反馈循环使数据偏见自我强化。

规模效应是数据偏见放大的第三个机制。算法系统的大规模应用使偏见的影响范围广泛。一个存在偏见的算法系统可能同时影响数百万人，偏见通过系统性的决策模式被扩散。规模效应使数据偏见的影响更为深远，也更难通过个案救济来矫正。

五、数据偏见的治理困境

数据偏见的治理，面临多重困境。

数据清洗的局限是第一个困境。数据清洗可以识别和矫正部分数据偏见，但难以解决历史偏见和代理偏见。历史偏见深植于社会结构之中，难以通过数据清洗来消除。代理偏见使敏感特征被排除后偏见仍然存在，难以通过简单的特征排除来矫正。

公平性约束的困难是第二个困境。公平性约束可以在模型训练中引入公平性目标，但公平性定义本身存在争议。统计均等、机会均等和结果均等之间的张力，使公平性约束难以确定。公平性约束可能以牺牲预测性能为代价，使企业缺乏动力。

标注质量的控制是第三个困境。标注偏见需要通过标注质量控制来矫正，但标注质量控制面临成本、规模和一致性的挑战。大规模数据标注需要大量人力，标注质量控制难以全面覆盖。标注偏见的主观性使标注质量控制难以标准化。

持续监测的困难是第四个困境。数据偏见随数据更新和模型再训练而变化，需要持续监测。但持续监测需要资源、能力和制度支撑，企业缺乏动力，监管缺乏手段。持续监测的困难使数据偏见难以被及时发现和矫正。

六、结论与启示

训练数据中的历史偏见来源于代表性不足、标注偏见、历史歧视记录和代理变量等渠道。这些偏见通过统计规律、反馈循环和规模效应被算法系统放大，造成比原始数据更严重的歧视。数据偏见具有隐蔽性、结构性和自我强化性特征，使其难以通过简单的数据清洗来矫正。

治理数据偏见，需要数据采集的代表性保障、标注过程的质量控制、公平性约束的模型训练和持续监测的反馈机制的多管齐下。数据采集应当保障代表性，避免系统性排除某些群体。标注过程应当建立质量控制机制，减少标注偏见。模型训练应当引入公平性约束，平衡预测性能和公平性。持续监测应当建立反馈机制，及时发现和矫正数据偏见。

数据偏见的治理，不仅关系到算法系统的公平性，也关系到社会正义和民主价值。一个存在系统性数据偏见的社会，难以称为公正的社会；一个存在系统性数据偏见的算法系统，难以称为可信的技术。数据偏见的治理，是人工智能时代社会正义的核心议题，也是技术伦理和公共政策的紧迫任务。

参考文献源：<strong>gxseedcorp.cn</strong> <strong>www.gxseedcorp.cn</strong> <strong>m.gxseedcorp.cn</strong>