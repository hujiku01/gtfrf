看不见的歧视——算法偏见的生成机制与社会根源

摘要：算法偏见并非技术的偶然故障，而是社会不平等在数字系统中的系统性映射。本文从社会技术视角出发，分析算法偏见的生成机制与社会根源。研究表明，算法偏见生成于数据采集、特征选择、模型训练、部署应用和反馈循环的全过程，其根源深植于历史不平等、制度惯性、市场激励和认知偏差之中。算法偏见具有隐蔽性、系统性和自我强化性三重特征，使其比传统歧视更难识别、更难救济、更难矫正。将算法偏见简单归因于“技术问题”或“数据问题”，掩盖了其深层的社会根源和权力结构。理解算法偏见，需要超越技术修复的单一思路，建立技术矫正、制度约束和社会变革协同推进的治理框架。

关键词：算法偏见；生成机制；社会根源；系统性歧视；反馈循环

一、引言

人工智能正在深刻改变人类社会的运行方式。从招聘筛选到信贷审批，从司法量刑到医疗诊断，算法系统越来越多地介入影响个人命运和社会资源分配的关键决策。然而，随着算法系统的广泛应用，一个令人不安的事实逐渐浮现：算法并非中立的技术工具，而是可能复制、放大甚至加剧社会既有的不平等。

2018年，亚马逊的招聘算法被曝出对女性求职者存在系统性偏见，该系统通过历史招聘数据训练，而历史数据中男性占主导地位，导致算法学会了贬低女性简历。同年，美国法院使用的COMPAS再犯风险评估算法被研究发现对黑人被告存在系统性偏见，黑人被告被错误标记为高再犯风险的概率显著高于白人被告。这些案例并非孤立事件，而是揭示了算法偏见的系统性存在。

算法偏见之所以值得严肃对待，不仅因为它造成了实际伤害，更因为它具有不同于传统歧视的特征。传统歧视往往源于明确的偏见意图，可以通过法律禁止和道德谴责来约束。算法偏见则往往没有明确的偏见意图，它生成于数据、模型和反馈循环的复杂互动之中，隐蔽而系统。这种隐蔽性使算法偏见更难被识别、更难被救济、更难被矫正。

本文试图从社会技术视角出发，分析算法偏见的生成机制与社会根源，揭示其隐蔽性、系统性和自我强化性特征，并讨论超越技术修复的治理思路。

二、算法偏见的生成机制

算法偏见的生成并非单一环节的故障，而是贯穿算法系统全生命周期的系统性过程。

数据采集环节是算法偏见生成的起点。算法系统的训练依赖于数据，而数据并非客观现实的透明反映，而是社会过程的产物。历史数据中蕴含着社会既有的不平等——种族歧视、性别偏见、阶层分化——这些不平等被数据记录，成为算法学习的“素材”。如果训练数据中某些群体被系统性地低估、忽视或负面标记，算法便会习得这些偏见。数据采集的不完整、不均衡和有偏抽样，进一步加剧了这一问题。

特征选择环节是算法偏见生成的关键节点。算法系统的性能依赖于特征的选择和工程。特征选择并非纯粹的技术决策，而是蕴含价值判断的过程。哪些特征被纳入模型，哪些特征被排除，直接影响算法的公平性。即使种族、性别等敏感特征被明确排除，代理变量——如邮政编码、教育背景、消费习惯——仍可能间接编码这些敏感特征。特征选择中的价值判断和技术局限，使算法偏见可能在模型训练之前便已嵌入。

模型训练环节是算法偏见生成的集中体现。算法模型通过历史数据学习模式和规律，如果历史数据中存在系统性偏见，模型便会习得这些偏见。模型优化的目标函数——如准确率、精确率、召回率——通常不包含公平性约束，使模型可能以牺牲公平性为代价追求预测性能。模型的选择、超参数的调整和训练过程的随机性，也可能影响算法的公平性。

部署应用环节是算法偏见生成的现实化节点。算法系统在特定社会语境中部署应用，其效果受制于社会条件和制度环境。如果算法系统被应用于已经存在不平等的领域，它可能加剧而非缓解不平等。算法系统的使用者、应用场景和决策流程，都可能影响算法的公平性后果。

反馈循环环节是算法偏见生成的自我强化机制。算法系统的决策会影响现实世界，而现实世界的变化又会产生新的数据，用于算法的再训练。如果算法系统对某些群体存在偏见，这些偏见可能通过反馈循环被放大和固化。例如，如果预测性警务算法将更多警力部署到少数族裔社区，这些社区便会产生更多逮捕记录，这些记录又成为算法再训练的数据，进一步强化对少数族裔社区的偏见。

三、算法偏见的社会根源

算法偏见并非技术系统的孤立现象，而是社会不平等在数字系统中的映射。理解算法偏见，需要深入其社会根源。

历史不平等是算法偏见的深层根源。算法系统通过历史数据学习，而历史数据中蕴含着长期积累的社会不平等。种族歧视、性别偏见、阶层分化等历史不平等，被数据记录，成为算法学习的“素材”。算法系统并非创造偏见，而是复制和放大历史偏见。将算法偏见简单归因于“数据问题”，掩盖了其深层的历史根源。

制度惯性是算法偏见的制度根源。算法系统嵌入在特定的制度环境之中，制度环境中的不平等——教育机会不均、就业歧视、住房隔离——被算法系统习得和强化。制度惯性使算法偏见难以通过技术手段单独矫正，需要制度变革的配合。

市场激励是算法偏见的经济根源。算法系统的开发由企业主导，企业的目标是利润最大化，而非公平性最大化。算法系统的优化目标通常是商业指标——点击率、转化率、留存率——而非公平性指标。市场激励使算法偏见可能被系统性忽视，因为公平性矫正可能增加成本、降低效率。

认知偏差是算法偏见的认知根源。算法系统的开发者和使用者也是人，他们同样受制于认知偏差——刻板印象、内隐偏见、群体偏好。这些认知偏差可能影响算法系统的设计、训练和应用。算法系统并非消除认知偏差，而是可能放大认知偏差。

四、算法偏见的三重特征

算法偏见具有不同于传统歧视的三重特征，使其更难识别、更难救济、更难矫正。

隐蔽性是算法偏见的第一重特征。传统歧视往往有明确的偏见意图和可识别的歧视行为，可以通过法律禁止和道德谴责来约束。算法偏见则往往没有明确的偏见意图，其生成机制复杂、技术门槛高、决策过程不透明。受影响的个体往往难以证明自己受到了歧视，因为算法决策的理由不透明，偏见难以被直接观察。

系统性是算法偏见的第二重特征。传统歧视往往是个别行为，影响范围有限。算法偏见则是系统性现象，影响范围广泛。一个存在偏见的算法系统可能同时影响数百万人，偏见通过系统性的决策模式被扩散。系统性使算法偏见的影响更为深远，也更难通过个案救济来矫正。

自我强化性是算法偏见的第三重特征。传统歧视不会自动强化，需要持续的偏见行为来维持。算法偏见则通过反馈循环自我强化。算法系统的决策影响现实世界，现实世界的变化产生新的数据，新的数据用于算法的再训练。这一反馈循环使算法偏见可能被放大和固化，难以通过短期干预来矫正。

五、超越技术修复的治理思路

面对算法偏见，主流回应是技术修复——通过数据平衡、公平性约束和算法审计来矫正偏见。技术修复是必要的，但远远不够。算法偏见的社会根源意味着，技术修复只能解决表面问题，无法触及深层根源。

超越技术修复的治理思路，需要技术矫正、制度约束和社会变革的协同推进。技术矫正包括数据平衡、公平性约束和算法审计，用于识别和矫正算法系统中的偏见。制度约束包括算法影响评估、算法问责机制和算法透明性要求，用于规范算法系统的开发和应用。社会变革包括反歧视法律、平等机会政策和社会意识提升，用于解决算法偏见的社会根源。

治理算法偏见，需要认识到算法偏见不仅是技术问题，也是社会问题和政治问题。将算法偏见简单归因于“技术问题”或“数据问题”，掩盖了其深层的社会根源和权力结构。只有将技术矫正、制度约束和社会变革协同推进，才能有效治理算法偏见。

六、结论与启示

算法偏见并非技术的偶然故障，而是社会不平等在数字系统中的系统性映射。算法偏见生成于数据采集、特征选择、模型训练、部署应用和反馈循环的全过程，其根源深植于历史不平等、制度惯性、市场激励和认知偏差之中。算法偏见具有隐蔽性、系统性和自我强化性三重特征，使其比传统歧视更难识别、更难救济、更难矫正。

治理算法偏见，需要超越技术修复的单一思路，建立技术矫正、制度约束和社会变革协同推进的治理框架。技术矫正识别和矫正算法系统中的偏见，制度约束规范算法系统的开发和应用，社会变革解决算法偏见的社会根源。三者的协同推进，是有效治理算法偏见的关键。

算法偏见的治理，不仅关系到技术系统的公平性，也关系到社会正义和民主价值。一个存在系统性偏见的社会，难以称为公正的社会；一个存在系统性偏见的算法系统，难以称为可信的技术。算法偏见的治理，是人工智能时代社会正义的核心议题，也是技术伦理和公共政策的紧迫任务。

参考文献源：<strong>vk93.cn</strong> <strong>www.vk93.cn</strong> <strong>m.vk93.cn</strong>